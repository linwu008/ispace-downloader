# v0.1 验收记录

验收日期：2026-09-11。用户明确确认“已验收”。

## 实际环境证据

- [x] 真实账号登录成功，课程接口读取到 10 门课程。
- [x] 一门课程绑定专用本地文件夹，识别到 12 个附件链接。
- [x] 首次成功同步：新增 8、已有 4、失败 0。
- [x] 正式 Windows 计划任务再次同步：新增 0、已有 12、失败 0，任务返回码为 0。
- [x] 独立计划任务进程成功复用加密登录会话与已保存课程绑定。
- [x] 正式每日计划启用：北京时间每天 20:00，首个计划执行时间为 2026-09-11 20:00。
- [x] 桌面和手机尺寸的网页检查通过，无 JavaScript 错误或水平溢出。
- [x] GitHub 私有仓库已推送，发布前运行 Windows / Python 3.13 自动测试。

这些数据来自实际程序的同步记录与 Windows 任务查询，不包含账号密码、课程文件名或私有学习内容。

## 自动化覆盖

本机 38 项测试通过，覆盖两门课程的文件隔离、嵌套文件、同名更新、文件改名和删除后的恢复、弱 ETag 内容校验、磁盘写入失败重试、登录失效、课程及论坛分页、附件类型、异常附件、凭据加密、跨进程互斥、定时任务 XML，以及不同 Windows 启动环境下的数据目录持久化。

Windows 计划任务 XML 另经实际注册与执行验证；独立验证任务已删除。正式任务名称为 `iSpaceDownloader-v0.1`。

## 验证边界

- 真实下载验收以当前绑定的一门课程为样本；其余课程及所有附件类型尚未逐一人工对照。
- 两门课程隔离、同名更新、删除后补下载等边界场景有模拟测试覆盖，未对用户现有学习文件执行删除或改写试验。
- 已启用并核对 `StartWhenAvailable` 补查设置，尚未做真实关机跨越计划时间的长时间测试。
- 每日检查要求电脑开机联网，且保存凭据的 Windows 用户已登录；用户手动注销或验证码可能中止自动登录。
- 后续网站主题、权限或接口变化仍需根据失败记录适配。
